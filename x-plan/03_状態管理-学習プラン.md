# 状態管理 学習プラン（ClearML 実態ベース）

## まとめ

| 項目 | 内容 |
| --- | --- |
| 目的 | ClearML のコードを読める／直せる状態になること。**ClearML で実際に使われている API だけ**を、使用頻度と依存順に学ぶ |
| 根拠 | [x-QA/04 未掲載調査](../x-QA/04_ClearMLで使用中の状態管理API-辞書未掲載調査.md) + 本資料作成時に再計測した ClearML `src` 全 1,569 ファイルの import 集計 |
| 対象コード | `/Users/yasu/work/mergelog/learn-ClearML-pro/src` |
| 既存辞書との関係 | [x-docs/00_70 逆引き-状態管理](../x-docs/00_70_逆引き-状態管理.md) は「一般論・ベストプラクティスの逆引き辞書」として**現状維持**。本資料は「ClearML を読むための順序付き学習リスト」で役割が異なる |
| 学習項目数 | 全 78 項目 / STEP 1〜10 |
| 到達目安 | STEP 1〜3 で ClearML の状態フローを 1 本追える。STEP 1〜7 で日常の調査・実装ができる |
| 除外方針 | ClearML での使用が 0 件の API は「[学習しないもの](#学習しないもの-clearml-で使用-0-件)」にまとめ、意図的に学習対象から外す |
| 注意 | 数値は計測値。詳細は末尾の[計測メモ](#計測メモ)参照 |

---

## この資料の位置づけ

- `00_70 逆引き辞書` は「やりたいことから API を引く」ための一般資料。**これはこのまま維持する**。
- 一方で ClearML 学習にあたり
  - 辞書に載っていても **ClearML で 0 件の API**（`rxMethod` / `tapResponse` / `resource()` など）を学ぶのは効率が悪い
  - 逆に **辞書に載っていないのに 370 箇所使われている API**（`ngrxPush`）がある
  という実態のズレがある。
- そこで「ClearML を読むための学習順序」を本資料に切り出した。辞書は引く物、本資料は**潰していく物**。

---

## 表の見方

| 列 | 内容 |
| --- | --- |
| No. | 通し番号。STEP をまたいで連番 |
| ✔ | 完了したら `[ ]` を `[x]` に書き換える |
| 関数名 | 学習対象の API。関連が強いものは 1 行にまとめてある |
| RxJS / NgRx / Signal | 提供元の分類（下記凡例） |
| 機能概要 | その API が何をする物か |
| 学習概要 | 何を押さえれば良いか。躓きやすい点 |
| 備考 | ClearML での使用量と、逆引き辞書への掲載有無 |

### 分類の凡例

| 表記 | 提供元 |
| --- | --- |
| `RxJS [func]` | `rxjs` の Observable 生成関数 |
| `RxJS [pipe]` | `rxjs/operators` の operator |
| `RxJS [Subject]` | 値の通知と現在値保持を担う Observable |
| `NgRx Store` | `@ngrx/store` |
| `NgRx Effects` | `@ngrx/effects` |
| `NgRx operators` | `@ngrx/operators` |
| `NgRx component` | `@ngrx/component` |
| `NgRx SignalStore` | `@ngrx/signals` |
| `NgRx SignalStore Events` | `@ngrx/signals/events` |
| `NgRx testing` | `@ngrx/store/testing` / `@ngrx/effects/testing` |
| `Angular Signal` | `@angular/core` |
| `Angular interop` | `@angular/core/rxjs-interop` |
| `Angular common` | `@angular/common` |
| `Angular common/http` | `@angular/common/http` |
| `外部ライブラリ` | NgRx / Angular 本体以外 |

### 備考欄の読み方

- `196ファイル` = その識別子を **import しているファイル数**（x-QA/04 と同じ数え方）
- `1,026箇所` = **文字列の出現回数**
- `辞書: 掲載あり` = 00_70 に専用セクションがある / `辞書: 未掲載` = 項目自体が無い

---

## 最初に読む教材コード

STEP 1〜3 は、この **1 機能ぶんの一式（合計 185 行）** を読みながら進めるのが最短。
`src/app/webapp-common/shared/queue-create-dialog/`

| ファイル | 行数 | 登場する API |
| --- | ---: | --- |
| `queue-create-dialog.actions.ts` | 12 | `createAction` / `props` |
| `queue-create-dialog.reducer.ts` | 31 | `createReducer` / `on` /<br>`createFeatureSelector` / `createSelector` |
| `queue-create-dialog.effects.ts` | 72 | `Actions` / `createEffect` / `ofType` /<br>`mergeMap` / `switchMap` / `catchError` |
| `queue-create-dialog.providers.ts` | 9 | `provideState` / `provideEffects` |
| `queue-create-dialog.component.ts` | 61 | `inject(Store)` / `dispatch` /<br>`select` と `selectSignal` の両方が出る |

STEP 9（SignalStore）の教材はこの 2 本。

| ファイル | 登場する API |
| --- | --- |
| `src/app/webapp-common/core/state/view.events.ts` | `eventGroup` / `type` |
| `src/app/webapp-common/core/state/view.store.ts` | `signalStoreFeature` /<br>`withEventHandlers` / `Events.on` |

> **読む前の注意**: ClearML は selector を `*.selectors.ts` に分離しないことが多い。
> `*.actions.ts` 48 本 / `*.reducer*.ts` 45 本に対し `*.selectors.ts` は 5 本しか無い。
> selector を探すときは reducer ファイルの中も見ること。

---

## STEP 1: Store の一往復（dispatch → reducer → selector）

**目的**: ClearML の状態が「どこで変わり、どこで読まれるか」を 1 本追えるようにする。**必修**。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 1 | `[x]` | `inject(Store)` | NgRx Store | アプリ全体の状態を<br>保持する単一の Store | まず `dispatch` /<br>`select` /<br>`selectSignal` の<br>3 つだけ覚える | 196ファイル<br>辞書: 掲載あり |
| 2 | `[x]` | `store.dispatch()` | NgRx Store | Action を Store へ送る。<br>状態変更の唯一の入口 | 「どう変えるか」ではなく<br>「何が起きたか」を送る。<br>コード追跡の起点になる | 1,026箇所<br>160ファイル<br>辞書: 掲載あり |
| 3 | `[x]` | `createAction()` | NgRx Store | Action を定義する | `'[FEATURE] EVENT'` 命名。<br>定義した定数を grep すると<br>reducer と effects の<br>両方が見つかる | 44ファイル<br>608箇所<br>**辞書: 未掲載** |
| 4 | `[ ]` | `props<T>()` | NgRx Store | Action のペイロード<br>型を宣言する | `createAction('...',`<br>`props<{id: string}>())`。<br>ペイロード無しは第2引数なし | 46ファイル<br>**辞書: 未掲載** |
| 5 | `[x]` | `createActionGroup()`<br>`emptyProps()` | NgRx Store | Action を<br>まとめて定義する<br>新しめの書き方 | ClearML は `createAction` 44 と<br>group 7 が併存。<br>新規追加時にどちらへ<br>合わせるかは要確認 | 7 / 7ファイル<br>**辞書: 未掲載** |
| 6 | `[x]` | `createReducer()`<br>`on()` | NgRx Store | Action から次の state を<br>純粋関数で作る | 副作用は書かない。<br>`on` は `@ngrx/store` と<br>`@ngrx/signals/events` の<br>両方にあり import 元の<br>確認が要る | 41 / 47ファイル<br>辞書: 掲載あり |
| 7 | `[x]` | `createFeatureSelector()` | NgRx Store | feature state の<br>入口 selector | `provideState('key', ...)` の<br>key 文字列と一致させる<br>必要がある | 12ファイル<br>**辞書: 未掲載** |
| 8 | `[x]` | `createSelector()` | NgRx Store | state から表示用の値を<br>導出する（メモ化あり） | 入力 selector を合成する。<br>481箇所あり ClearML の<br>派生ロジックの中心 | 36ファイル<br>481箇所<br>辞書: 掲載あり |
| 9 | `[x]` | `store.select()` | NgRx Store | selector を<br>Observable として読む | **ClearML の主流**。<br>`\| ngrxPush` とセットで<br>読むのが基本形 | 925箇所<br>136ファイル<br>辞書: 掲載あり |
| 10 | `[x]` | `store.selectSignal()` | NgRx Store | selector を<br>Signal として読む | Observable 版と併存。<br>新規に書くなら原則こちら | 303箇所<br>99ファイル<br>辞書: 掲載あり |
| 11 | `[x]` | `select()` **[pipe]** | NgRx Store | `store.pipe(select(sel))`<br>の旧形式 | 読めれば十分。<br>新規では使わない | 3ファイル<br>**辞書: 未掲載** |
| 12 | `[x]` | `createFeature()` | NgRx Store | reducer と selector を<br>まとめて定義する | ClearML では少数派。<br>actions / reducer を<br>分離する形が主流 | 5ファイル<br>**辞書: 未掲載** |

---

## STEP 2: 画面へ出す（テンプレート表示）

**目的**: Observable / Signal をテンプレートに出す 2 系統を区別する。**必修**。辞書の最大の欠落箇所。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 13 | `[x]` | `ngrxPush`<br>（`PushPipe`） | NgRx component | Observable を<br>テンプレートへ出す pipe | **ClearML の既定**。<br>zoneless / OnPush 前提で<br>`markForCheck` を自前で呼ぶ。<br>import 忘れに注意 | 370箇所<br>60ファイル<br>**辞書: 未掲載**<br>(QA 優先度1) |
| 14 | `[x]` | `async`<br>（`AsyncPipe`） | Angular common | 標準の<br>Observable 表示 pipe | 6 箇所のみ。<br>`ngrxPush` との違いを<br>1 回確認すれば良い | 3ファイル<br>**辞書: 未掲載** |
| 15 | `[x]` | `toSignal()` | Angular interop | Observable を<br>Signal に変換する | `initialValue` の扱いと、<br>注入コンテキストが<br>必要な点に注意 | 32ファイル<br>辞書: 掲載あり |
| 16 | `[x]` | `toObservable()` | Angular interop | Signal を<br>Observable に変換する | Signal から RxJS へ戻す時。<br>内部は effect ベースで<br>同期的には流れない | 16ファイル<br>辞書: 掲載あり |

---

## STEP 3: Effects と非同期

**目的**: ClearML の非同期は全面的に Effects に寄っている。ここが読めないと API 呼び出しが追えない。**必修**。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 17 | `[x]` | `Actions` | NgRx Effects | 全 Action が流れる<br>Observable | `inject(Actions)` で取得。<br>reducer の後に流れてくる | 45ファイル<br>辞書: 掲載あり |
| 18 | `[x]` | `createEffect()` | NgRx Effects | Action を受けて<br>副作用を実行する | 外側の pipe に `catchError`<br>や `take` を置くと Effect が<br>止まる。内部 Observable 側で<br>処理する | 41ファイル<br>290箇所<br>辞書: 掲載あり |
| 19 | `[x]` | `ofType()` | NgRx Effects | 対象 Action だけを通す | Effect の入口。<br>まずこれで絞る | 43ファイル<br>辞書: 掲載あり |
| 20 | `[x]` | `{dispatch: false}` | NgRx Effects | Action を返さない<br>Effect の宣言 | Router 遷移・ログ等。<br>指定漏れは無限ループの元 | 19箇所<br>辞書: 掲載あり |
| 21 | `[x]` | `concatLatestFrom()` | NgRx operators | Action 発生時に<br>selector の最新値を<br>**遅延**取得する | factory が遅延評価される点が<br>`withLatestFrom` との差。<br>ClearML は `iif` と組んで<br>参照先 selector を出し分ける | 40ファイル<br>辞書: 掲載あり |
| 22 | `[x]` | `withLatestFrom()` | RxJS **[pipe]** | 他 Observable の<br>最新値を添える | 即時購読される。<br>Effect では基本<br>`concatLatestFrom` を使う | 25ファイル<br>辞書: 掲載あり |
| 23 | `[x]` | `switchMap()` | RxJS **[pipe]** | 新しい入力で<br>前の内部購読を破棄 | 検索・画面切替。<br>ClearML の Effects の主流 | 64ファイル<br>辞書: 掲載あり |
| 24 | `[x]` | `mergeMap()` | RxJS **[pipe]** | 並行実行。<br>完了順は不定 | ClearML では「複数 Action を<br>配列で返す」用途でも多用<br>（No.53 参照） | 42ファイル<br>辞書: 掲載あり |
| 25 | `[x]` | `concatMap()`<br>`exhaustMap()` | RxJS **[pipe]** | 直列化 /<br>実行中は新規を無視 | 各 2 ファイルのみ。<br>4 兄弟の違いの理解だけで可 | 各2ファイル<br>辞書: 掲載あり |
| 26 | `[x]` | `catchError()` | RxJS **[pipe]** | エラーを<br>失敗 Action に変換する | **内部 Observable 側**に置く。<br>`of(failAction)` を返すのが定形 | 50ファイル<br>辞書: 掲載あり |
| 27 | `[x]` | `of()`<br>`EMPTY` | RxJS **[func]** | 値から Observable /<br>何も流さず完了 | `catchError` の戻り値、<br>Effect の打ち切りで頻出 | 55 / 16ファイル<br>辞書: 例中のみ |
| 28 | `[x]` | `map()`<br>`filter()`<br>`tap()` | RxJS **[pipe]** | 変換 / 選別 / 副作用 | 全 operator 中で最頻出。<br>`tap` は調査時の<br>ログ差し込みにも使う | 113 / 110 / 38<br>ファイル<br>辞書: 掲載あり |

---

## STEP 4: 購読の寿命管理

**目的**: ClearML には新旧 2 方式が併存している。どちらも読めて、新規では新方式を選べるようにする。**必修**。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 29 | `[x]` | `takeUntilDestroyed()` | Angular interop | DI コンテキストの破棄で<br>自動 unsubscribe | 新方式。constructor の外で<br>使うなら `DestroyRef` を<br>引数で渡す | 61ファイル<br>107箇所<br>辞書: 掲載あり |
| 30 | `[x]` | `DestroyRef` | Angular Signal | 破棄タイミングの参照 | `takeUntilDestroyed(`<br>`this.destroyRef)` 用 | 21ファイル<br>辞書: 言及のみ<br>(takeUntilDestroyed項) |
| 31 | `[x]` | `Subscription` | RxJS | 購読を手動で束ねて<br>まとめて解除する | 旧方式。`new Subscription()`<br>+ `add()` + `ngOnDestroy` で<br>`unsubscribe()`。<br>ClearML に多数残存 | 33ファイル<br>18箇所<br>**辞書: 未掲載** |
| 32 | `[x]` | `Subject`<br>`BehaviorSubject` | RxJS **[Subject]** | イベントハブ /<br>現在値を保持する | `refresh.service.ts` 等。<br>`BehaviorSubject` は<br>初期値が必須 | 11 / 9ファイル<br>辞書: 掲載あり<br>(BehaviorSubject) |
| 33 | `[x]` | `take()`<br>`first()` | RxJS **[pipe]** | n 件で完了 /<br>条件一致の 1 件 | Effect の外側 pipe に<br>置かない。`first()` は<br>0 件だとエラーになる | 43 / 3ファイル<br>辞書: 掲載あり |
| 34 | `[x]` | `lastValueFrom()`<br>`firstValueFrom()` | RxJS **[func]** | Observable → Promise | `await` したい場面のみ。<br>complete しない<br>Observable では解決しない | 2 / 1ファイル<br>**辞書: 未掲載** |

---

## STEP 5: 時間制御・選別 operator

**目的**: 検索入力・スクロール・再描画抑制など、UI 側の頻出処理を読めるようにする。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 35 | `[x]` | `debounceTime()` | RxJS **[pipe]** | 入力が止まるまで待つ | 検索入力の定番。<br>61 ファイルと非常に多い | 61ファイル<br>辞書: 掲載あり |
| 36 | `[x]` | `debounce()` | RxJS **[pipe]** | 待ち時間を Observable で<br>**動的に**決める | ClearML は値によって<br>0ms / 5000ms を切り替える。<br>`debounceTime` では書けない | 7ファイル<br>**辞書: 未掲載**<br>(名前のみ) |
| 37 | `[x]` | `distinctUntilChanged()` | RxJS **[pipe]** | 前回と同値なら流さない | 既定は参照比較。<br>オブジェクトは比較関数が要る | 49ファイル<br>辞書: 掲載あり |
| 38 | `[x]` | `distinctUntilKeyChanged()` | RxJS **[pipe]** | 指定キーだけで<br>同値判定する | `distinctUntilKeyChanged`<br>`('id')` の形 | 4ファイル<br>**辞書: 未掲載** |
| 39 | `[x]` | `throttleTime()`<br>`auditTime()` | RxJS **[pipe]** | 期間の先頭を採る /<br>期間の**末尾**を採る | 採る位置が逆。<br>スクロール・リサイズ系で<br>どちらを使うかが挙動を変える | 6 / 3ファイル<br>辞書: throttleTime<br>のみ掲載 |
| 40 | `[x]` | `startWith()`<br>`skip()` | RxJS **[pipe]** | 初期値を先に流す /<br>先頭 n 件を捨てる | `skip(1)` は<br>「購読直後の現在値を無視」<br>の定番 | 16 / 10ファイル<br>辞書: startWith<br>のみ掲載 |
| 41 | `[x]` | `pairwise()` | RxJS **[pipe]** | 前回値と今回値を<br>組にする | 差分検知。<br>初回は流れない | 5ファイル<br>**辞書: 未掲載** |
| 42 | `[x]` | `finalize()` `delay()`<br>`timeout()` `bufferTime()` | RxJS **[pipe]** | 後始末 / 遅延 /<br>時間切れ /<br>一定時間分を束ねる | 各 1〜3 ファイルと少数。<br>読めれば可 | 3/1/1/1ファイル<br>辞書: 名前のみ |

---

## STEP 6: Observable の源流と合流

**目的**: 辞書が合流系（`combineLatest` 等）しか扱っていない部分を埋める。「Observable はどこから生まれるか」。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 43 | `[x]` | `fromEvent()` | RxJS **[func]** | DOM イベントを<br>Observable 化する | `toSignal(fromEvent(`<br>`window, 'resize'))`<br>の形で使われる | 17ファイル<br>**辞書: 未掲載** |
| 44 | `[x]` | `interval()`<br>`timer()` | RxJS **[func]** | 周期通知 /<br>遅延 + 周期通知 | `timer(0, N)` は<br>「即時 + 定期」。<br>ポーリングの起点（No.52） | 13 / 7ファイル<br>**辞書: 未掲載** |
| 45 | `[x]` | `from()` | RxJS **[func]** | Promise や配列を<br>Observable 化する | 配列を渡すと<br>要素ごとに流れる | 8ファイル<br>**辞書: 未掲載** |
| 46 | `[x]` | `throwError()` | RxJS **[func]** | エラーを流す | `throwError(() => err)` の<br>**関数形**が現行の書き方。<br>interceptor で頻出 | 5ファイル<br>**辞書: 未掲載** |
| 47 | `[x]` | `iif()` | RxJS **[func]** | 条件で購読先を<br>切り替える | `concatLatestFrom` と組んで<br>Action 種別ごとに参照 selector<br>を出し分ける ClearML の頻出形 | 4ファイル<br>**辞書: 未掲載** |
| 48 | `[x]` | `fromFetch()` | RxJS **[func]** | `HttpClient` を通さない<br>直 fetch | `rxjs/fetch` から import。<br>**interceptor が効かない**<br>点に注意 | 4ファイル<br>**辞書: 未掲載** |
| 49 | `[x]` | `combineLatest()`<br>`forkJoin()` | RxJS **[func]** | 最新値の組合せ /<br>全 complete 後に 1 回 | `forkJoin` は 1 本でも<br>complete しないと発火しない | 40 / 10ファイル<br>辞書: 掲載あり |
| 50 | `[x]` | `merge()` `concat()`<br>`combineLatestWith()` | RxJS<br>**[func]** / **[pipe]** | 合流各種 | 各 1〜3 ファイル。<br>辞書の説明で足りる | 2/1/3ファイル<br>辞書: 掲載あり |

---

## STEP 7: ClearML 固有の頻出パターン

**目的**: 辞書の逆引き表からは到達できない、ClearML 特有の組み合わせを型として覚える。**読解に必須**。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 51 | `[x]` | `expand()` + `reduce()` | RxJS **[pipe]** | 次ページを自己再帰で<br>取り切り、最後に<br>1 本へ畳む | **一覧系 Effect の中核**。<br>`expand` が終了条件で<br>`EMPTY` を返すまで再帰する | 6 / 6ファイル<br>**辞書: 未掲載**<br>(QA 優先度2) |
| 52 | `[x]` | `timer()` +<br>`takeWhile()` +<br>`takeUntil(ofType())` | RxJS **[pipe]**<br>+ NgRx Effects | 画面を離れるまで<br>定期取得し続ける | `takeWhile(pred, true)` の<br>第2引数 inclusive に注意。<br>`takeUntilDestroyed` では<br>表現できない停止条件 | takeUntil 4<br>takeWhile 3<br>ファイル<br>**辞書: 未掲載**<br>(QA 優先度3) |
| 53 | `[x]` | `mergeMap(() =>`<br>`[actionA, actionB])` | RxJS **[pipe]** | 1 つの結果から<br>複数 Action を<br>dispatch する | 配列を返すと各要素が<br>Action として個別に流れる。<br>ClearML の定型 | 27箇所<br>**辞書: 未掲載** |
| 54 | `[-]` | `retry({count, delay})`<br>`retryWhen()` | RxJS **[pipe]** | 再試行 | `retryWhen` は RxJS 7 で<br>非推奨（v8 で削除予定）。<br>同一リポジトリに新旧が<br>1 件ずつ残っている | 各1ファイル<br>辞書: retry のみ |
| 55 | `[-]` | `toArray()` | RxJS **[pipe]** | 全通知を配列化する | 主にテストコードで使用。<br>complete が前提 | 2ファイル<br>**辞書: 未掲載** |

---

## STEP 8: Angular Signal 系

**目的**: Store とは別に、コンポーネント内の状態は Signal で書かれている。ClearML は両方が併存する。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 56 | `[-]` | `signal()`<br>`set()` / `update()` | Angular Signal | 書き込み可能な状態 | オブジェクトの中身を<br>書き換えても検知しない。<br>新しい参照を作る | 91ファイル<br>辞書: 掲載あり |
| 57 | `[-]` | `computed()` | Angular Signal | 依存 Signal から<br>派生値を作る | 派生は `effect` ではなく<br>`computed`。328 箇所使用 | 143ファイル<br>328箇所<br>辞書: 掲載あり |
| 58 | `[-]` | `effect()` | Angular Signal | Signal 世界の外側へ<br>副作用を起こす | **NgRx の Effects とは別物**。<br>状態の伝播に使わない | 82ファイル<br>150箇所<br>辞書: 掲載あり |
| 59 | `[x]` | `untracked()`<br>`EffectRef` | Angular Signal | 依存追跡から外す /<br>effect の手動破棄 | `effect` 内で「読むだけで<br>再実行トリガーにしたくない」<br>値に使う | 12 / 1ファイル<br>辞書: 言及のみ |
| 60 | `[x]` | `linkedSignal()`<br>`model()` | Angular Signal | 依存で再計算できる<br>書込可能 Signal /<br>親子の双方向 | `computed` との違い<br>（自分でも書ける）を押さえる | 12 / 9ファイル<br>辞書: linkedSignal<br>のみ掲載 |
| 61 | `[x]` | `input()` `output()`<br>`viewChild()` | Angular Signal | コンポーネント I/O・<br>Signal クエリ | 状態管理そのものではないが<br>ClearML 全域で使用。<br>Signal の読み書きの<br>練習になる | 194/128/71<br>ファイル<br>辞書: 対象外 |
| 62 | `[x]` | `rxResource()`<br>`httpResource()` | Angular interop /<br>Angular common/http | Resource 系の取得 API | 各 1 ファイルのみ。<br>**存在を知る程度でよい**。<br>`resource()` は 0 件 | 各1ファイル<br>辞書: 掲載あり |

---

## STEP 9: SignalStore と Events ブリッジ

**目的**: 使用箇所は少ないが、**NgRx Store と SignalStore を event で橋渡しする**という ClearML 固有の設計が絡む。仕組みを 1 回理解しておく。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 63 | `[ ]` | `signalStore()` | NgRx SignalStore | Signal ベースの<br>Store を定義する | 共有範囲は provider を<br>置く場所で決まる<br>（root か画面単位か） | 3ファイル<br>辞書: 掲載あり |
| 64 | `[ ]` | `withState()`<br>`withComputed()` | NgRx SignalStore | 状態の定義 /<br>派生値の定義 | `withState` の型がそのまま<br>Store の Signal になる | 4 / 3ファイル<br>辞書: 掲載あり |
| 65 | `[ ]` | `withMethods()`<br>`patchState()` | NgRx SignalStore | メソッド定義 /<br>状態の部分更新 | ClearML では各 2 / 1 ファイル<br>と少数。深追い不要 | 2 / 1ファイル<br>辞書: 掲載あり |
| 66 | `[ ]` | `signalStoreFeature()` | NgRx SignalStore | 再利用可能な<br>feature を自作する | `withViewBridge()` の実体。<br>feature を関数で切り出す形 | 3ファイル<br>**辞書: 未掲載** |
| 67 | `[ ]` | `eventGroup()`<br>`type()` | NgRx SignalStore<br>Events | event の定義と<br>ペイロード型 | `source` + `events` で定義。<br>**発火側**の API | 4 / 4ファイル<br>**辞書: 未掲載** |
| 68 | `[ ]` | `withEventHandlers()`<br>`Events` / `events.on()` | NgRx SignalStore<br>Events | event の受け口 | ClearML は受けた event を<br>`globalStore.dispatch()` へ<br>流すブリッジを組んでいる | 3 / 3ファイル<br>辞書: 言及のみ |
| 69 | `[ ]` | `injectDispatch()`<br>`Dispatcher` | NgRx SignalStore<br>Events | コンポーネントから<br>event を発火する | 各 1 ファイルのみ。<br>実例が少ないので<br>NgRx 公式も併読する | 各1ファイル<br>**辞書: 未掲載** |
| 70 | `[ ]` | `withReducer()` | NgRx SignalStore<br>Events | event を受けて<br>state を更新する | Store の reducer に相当。<br>受け手側の書き方 | 3ファイル<br>辞書: 掲載あり |
| 71 | `[ ]` | `withDevtools()` | 外部ライブラリ | SignalStore を<br>Redux DevTools に出す | `@angular-architects/`<br>`ngrx-toolkit` 製で<br>**NgRx 本体ではない** | 3ファイル<br>**辞書: 未掲載** |

---

## STEP 10: provider 登録とテスト

**目的**: 「この state はどこで登録されているのか」を辿れるようにする。テスト改修時にも必要。

| No. | ✔ | 関数名 | RxJS / NgRx / Signal | 機能概要 | 学習概要 | 備考 |
| ---: | :-: | --- | --- | --- | --- | --- |
| 72 | `[ ]` | `provideStore()`<br>`provideStoreDevtools()` | NgRx Store | ルートへの Store 登録 /<br>DevTools 連携 | 各 2 ファイル。<br>アプリ起動部だけ見れば良い | 各2ファイル<br>**辞書: 未掲載** |
| 73 | `[ ]` | `provideState()`<br>`provideEffects()` | NgRx Store /<br>NgRx Effects | feature 単位の遅延登録 | `*.providers.ts` に集約される。<br>第1引数の key 文字列が<br>`createFeatureSelector` と対応 | 24 / 26ファイル<br>**辞書: 未掲載** |
| 74 | `[ ]` | `StoreModule`<br>`EffectsModule` | NgRx Store /<br>NgRx Effects | NgModule 時代の登録 | 各 2 ファイルに残存。<br>読めれば可 | 各2ファイル<br>**辞書: 未掲載** |
| 75 | `[ ]` | `MetaReducer`<br>`USER_PROVIDED_`<br>`META_REDUCERS` | NgRx Store | 全 Action に割り込む<br>reducer のラッパー | 1 / 2 ファイル。<br>「reducer を通っていない<br>のに state が変わる」時に<br>疑う場所 | 1 / 2ファイル<br>**辞書: 未掲載** |
| 76 | `[ ]` | `provideMockStore()`<br>`MockStore` | NgRx testing | テストで Store を<br>差し替える | 33 ファイルで使用。<br>テスト改修時の必修 | 33 / 7ファイル<br>**辞書: 未掲載** |
| 77 | `[ ]` | `provideMockActions()` | NgRx testing | Effects テスト用の<br>Action ストリーム | 2 ファイル。<br>Effects のテストを書く時 | 2ファイル<br>**辞書: 未掲載** |
| 78 | `[ ]` | `ActionCreator`<br>`ActionReducer`<br>`ReducerTypes`<br>`MemoizedSelector`<br>`ActionReducerMap` | NgRx Store | 型定義 | 共通ユーティリティや<br>ジェネリックな reducer を<br>書く時に必要になる | 10/9/9/5/4<br>ファイル<br>**辞書: 未掲載** |

---

## 学習しないもの: ClearML で使用 0 件

辞書には載っているが、ClearML の `src` に **1 件も無い** もの。今は学習対象から外す。

| API | 分類 | 辞書 | 判断 |
| --- | --- | --- | --- |
| `rxMethod()` | NgRx SignalStore | 掲載あり（手厚い） | ClearML の非同期は Effects に全面的に寄せている。<br>SignalStore を**新規に書く**局面が来たら戻る |
| `tapResponse()` | NgRx operators | 掲載あり | 同上 |
| `withHooks()` `withProps()`<br>`withLinkedState()` | NgRx SignalStore | 掲載あり | 同上 |
| `resource()` | Angular | 掲載あり | 0 件。`rxResource` / `httpResource` が各 1 件のみ |
| `afterRenderEffect()` | Angular | 掲載あり | 0 件 |
| `debounced()` | Angular | 掲載あり | 0 件。時間制御は RxJS 側（No.35-36）で行われている |
| `scan()` `zip()` `zipWith()`<br>`mergeWith()` `concatWith()` | RxJS | 掲載あり | RxJS としては 0 件。<br>**注意**: `zip` / `mergeWith` は lodash の同名関数が<br>使われており、grep すると紛らわしい |
| `ngrxLet` | NgRx component | 未掲載 | 0 件。表示は `ngrxPush`（No.13）に統一されている |

---

## 進め方

- STEP 単位で進め、終わったら ✔ 列を `[x]` に書き換える。
- STEP 1〜3 は[最初に読む教材コード](#最初に読む教材コード)を開いたまま進めると早い。
- 知らない語が出たら、
  - 備考が `辞書: 掲載あり` → [00_70 逆引き辞書](../x-docs/00_70_逆引き-状態管理.md)の該当セクション
  - 備考が `辞書: 未掲載` → [x-QA/04](../x-QA/04_ClearMLで使用中の状態管理API-辞書未掲載調査.md) に代表箇所（ファイル名:行）が載っている
- 本資料は「ClearML でどう使われているか」に寄せてある。一般的なベストプラクティスの判断は 00_70 側を正とする。

---

## 計測メモ

| 項目 | 内容 |
| --- | --- |
| 計測日 | 2026-09-20 |
| 対象 | `learn-ClearML-pro/src` 配下の `*.ts` 1,569 本（テンプレートは `*.html` の pipe 出現のみ） |
| 「Nファイル」 | その識別子を import しているファイル数。x-QA/04 と同じ数え方 |
| 「N箇所」 | grep による文字列出現回数 |
| バージョン | ClearML: Angular 22.1.5 / RxJS 7.8.2 / NgRx 22.0.0（辞書の前提と一致） |
| 除外処理 | `store.select()` は `d3.select` 等の同名メソッドを除外して集計。<br>`zip` / `mergeWith` は lodash 由来であり RxJS としては 0 件と判定 |
| 限界 | import していない使用（再 export 経由など）は数えられていない。<br>数値は**優先順位を決めるための目安**であり、厳密な使用回数ではない |
