# 操作時に補助ストリームの最新値を取得

## 関数詳細

`withLatestFrom` は、元の Observable が値を流した時に、指定した Observable の最新値を組み合わせる。

## どういうケースで使用するか

ボタン操作時に、選択中の設定や現在の状態を一緒に使いたい場合。

## 注意点

指定した Observable が一度も値を流していない間は結果が流れない。初期値が必要なら `BehaviorSubject` などを使う。
