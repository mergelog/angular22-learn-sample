# 固定時間と値ごとの待機時間で入力を間引く

## 関数詳細

`debounceTime` は値が止まってから固定時間後に最新値を流す。`debounce` は値ごとに Observable を作り、その Observable が通知するまで次の値が来なければ最新値を流す。

## どういうケースで使用するか

固定の待ち時間なら `debounceTime`、入力内容などに応じて待ち時間を変えるなら `debounce` を使用する。

## 注意点

待機中に次の値が来ると直前の値は破棄される。即時に処理すべき操作には使用しない。
