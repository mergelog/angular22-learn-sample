## ▼ ng-maze

```bash
npx mergelog/ng-maze --all --mdh -p .
npx mergelog/ng-maze ExperimentsComponent --mdh
npx mergelog/ng-maze InlineEditComponent --parents --mdh --with-routes
```

## ▼ ng-wiring

- 注意; data-id="exection"のような動的idではは探せないので、別プロパティを探す
- selectorがマッチしないことが多いので、'___属性名と値____' だけ指定して経路選択肢から選んでもいい。  
  その場合、ターミナル上で 「route: .+$」をgrepすると見易い

```bash
npx github:mergelog/ng-wiring '___属性名と値____' --project stackup --selector '___selector___'

# 例
npx github:mergelog/ng-wiring 'data-id=nameField' --project stackup --selector 'body > sm-root > sm-app-shell > div > div > sm-common-experiments > div > as-split > as-split-area:nth-child(2) > sm-experiment-output > div > sm-experiment-info-header > div.d-flex.align-items-center > div.d-flex.align-items-center.experiment-name-cont > sm-inline-edit > div > div.input > form > input'
```

```bash canvas
npx github:mergelog/ng-wiring 'data-id="pipelineIcon"' --project stackup --selector '#mat-mdc-dialog-4 > div > div > sm-queue-create-dialog > sm-dialog-template > div > div.generic-container > div:nth-child(1) > mat-icon'

npx github:mergelog/ng-wiring 'data-id="createNewTag"' --selector '#mat-menu-panel-97 > div > span > div > button > span > span'

npx github:mergelog/ng-wiring '___属性名と値____' --selector '___selector___'
npx github:mergelog/ng-wiring '___属性名と値____' --selector '___selector___'
npx github:mergelog/ng-wiring '___属性名と値____' --selector '___selector___'
npx github:mergelog/ng-wiring '___属性名と値____' --selector '___selector___'
```

## ▼ /bulletin

事前にスキル作成
https://github.com/mergelog/learn-ClearML-pro/tree/work_20260912/.claude/skills

```bush AIコーディングエージェントチャット
/bulletin {処理や概要やUI振る舞いを指定}
```

## ▼ RxJS / NgRx / Signal系 逆引き辞書
https://zenn.dev/mergelog/articles/acb9a4a6140070#%E9%80%86%E5%BC%95%E3%81%8D%E8%BE%9E%E6%9B%B8

## ▼ npm_modulesを消す
npx npkill

## ▼ tree構造を書く
https://tree.nathanfriend.com/

## ▼ 日本語技術文書の文章規範
https://gist.github.com/k16shikano/fd287c3133457c4fd8f5601d34aa817d

## ▼ emmet
https://docs.emmet.io/cheat-sheet/
```
td*5, ul>li*5, ul>li.item$*5
```

## ▼ bash

- " > "を改行に変換するエイリアス
selspl() {
  printf '%s\n' "$1" | perl -pe 's/ > /\n/g'
}
> selspl 'aaa > bbb > ccc'

- スコープ指定（現行bashには影響させない）は括弧をつける
(cd .. && ls -l)

- .bashrc
alias claude-x='claude --model claude-opus-5 --effort max --enable-auto-mode --permission-mode auto'

## ▼ CSV
SmoothCSV

## ▼ http tool
Tarend

## ▼ rsync

- .git以外をコピー（最後の / 重要）

  `rsync -a --exclude='.git/' /home/___user___/work_2026/000-learn-ClearML-pro/ ./`  
  `rsync -a --exclude='.git/' /home/___user___/work_2026/001-learn-ClearML/ ./`

## ▼ npm

- node_modulesを消す
  npx npkill

## ▼ vscode

- 指定パスのコードを編集する
codeコマンドを使用する `code ~/.gitignore_global`

- 改行切り替え
[alt + z]

- 【VSCode】codeコマンド一覧
https://qiita.com/P-man_Brown/items/7c292ae21f6710a70108

- 拡張機能  
  + Comment Translate

- claude code 追加  
コマンドパレット（Cmd+Shift+P）で Claude Code: Open in Side Bar を実行。これだけで claudeCode.preferredLocation が sidebar に自動更新されます。

- タブサイズ、タブ幅  
設定から tabSizingFixedMaxWidth max:170, min:120
[Workbench] › [Editor: Tab Sizing] サイズを fixed にする

- ピン留めしたタブサイズ  
workbench.editor.pinnedTabSizing
→ shrink

- vscodeで、選択範囲を{}の基準で段階的に広げるショートカット  
「Expand Selection（選択範囲の拡大）」

macOS:
Control + Shift + Command + →

Windows / Linux:
Shift + Alt + →

- ターミナル① → ② → ③ のように切り替えるショートカット

  macOS:
  
  * 次のターミナル：`Shift + Cmd + ]`
  * 前のターミナル：`Shift + Cmd + [`
  
  Windows / Linux:
  
  * 次のターミナル：`Ctrl + PageDown`å
  * 前のターミナル：`Ctrl + PageUp`
  
  VS Code公式では `Terminal: Focus Next Terminal` / `Terminal: Focus Previous Terminal` に対応しています。([Visual Studio Code][1])
  
  なお、「ターミナルを横にSplitしている場合」は別です。
  
  * macOS：`Option + Cmd + ← / →`
  * Windows / Linux：`Alt + ← / →`
  
  つまり、普通に複数ターミナルを開いてタブ切り替えするなら、Macでは **`Cmd + Shift + [` / `]`** を覚えておけばOKです。
  
  [1]: https://code.visualstudio.com/docs/terminal/basics?utm_source=chatgpt.com "Terminal Basics"

- プレビュー設定

ファイルを固定するかの設定
workbench.editor.enablePreview

| 設定                                                 | 対象                    | `true` の動作    |
| -------------------------------------------------- | --------------------- | ------------- |
| `workbench.editor.enablePreview`                   | Explorerなど通常のファイルオープン | シングルクリックで一時表示 |
| `workbench.editor.enablePreviewFromCodeNavigation` | 定義へ移動などコードナビゲーション     | 移動先を一時表示      |
| `workbench.editor.enablePreviewFromQuickOpen`      | `⌘P` の Quick Open     | 選んだファイルを一時表示  |

基本 on, off, on

- 同じ文字列をいっぺんに選択

VS Codeで、ファイル内の「同じ文字列をいっぺんに選択」するならこれです。

| やりたいこと                | Mac                   | Windows / Linux            |
| --------------------- | --------------------- | -------------------------- |
| 選択中の文字列と同じものを**全部選択** | `⌘ + Shift + L`       | `Ctrl + Shift + L`         |
| 同じ文字列を**1個ずつ追加選択**    | `⌘ + D`               | `Ctrl + D`                 |
| 検索結果を全部選択             | `⌘ + F` → `⌥ + Enter` | `Ctrl + F` → `Alt + Enter` |

たとえば、

`userName` を1つ選択 → **`⌘ + Shift + L`**

とすると、そのファイル内の `userName` が全部マルチカーソル状態になります。そのまま文字を打てば**全部同時に書き換え**できます。

おそらく今欲しいのは **`⌘ + Shift + L`** です。

- グループをロック

markdownリンクのクリックで右ウインドウにリンク先を表示

以下設定（多分デフォルト）
```"markdown.links.openLocation": "currentGroup"```

そのうえで、

・VS Codeを左右2分割する  
・左に目次用 Markdown を置く  
・左側の右上 ...  
・Lock Group を選択  
・コマンドパレットなら View: Lock Editor Group  
・右側はロックしない  
  
という構成にします。

## ▼ git

- worktree

[/L00_初期etc/31_worktree使用方法.md](./L00_初期etc/31_worktree使用方法.md)

- .gitignore に記載せず ignoreする
  + 方法1: プロジェクト内の`.git/info/exclude`に記載（.gitignoreと同じように記載）
  + 方法2: `~/.gitignore_global`に記載

- git log
```bash
# 幅抑制
git log --oneline | less -S
# 表示数指定
git log --oneline -3
```

- fixup

途中でコンフリクトして「やっぱり全部やめたい」となったら、
git rebase --abort

```bash
# 1. 今の変更を全部ステージ
git add -A

# 2. afbac7f に吸収するための一時コミットを作る
git commit --fixup=afbac7f

# 3. afbac7f から現在までを並べ替えて、fixupを自動吸収
git rebase -i --autosquash afbac7f^

# 4. push
git push --force-with-lease origin main
```

```
_user_@hmhome:~/work_2026/learn-ClearML$ git add . 👈
_user_@hmhome:~/work_2026/learn-ClearML$ git commit --fixup c0f62f7 👈
[main 224b257] fixup! test aaa
 1 file changed, 2 insertions(+)
_user_@hmhome:~/work_2026/learn-ClearML$ git rebase -i --autosquash c0f62f7^ 👈
```

- 

例: 2つ前のコミットメッセージ変更、未コミット差分がない前提

```
git rebase -i HEAD~3
↓
# 表示は上から 古→新 の順なので注意
reword aaa1111 変更したいコミット // pickからrewordに変更
pick   bbb2222 1つ前
pick   ccc3333 最新
↓
修正
↓ 
git push --force-with-lease

```

### コミットメッセージ/commit message

[L00_初期etc/30_コミットメッセージ.md](./L00_初期etc/30_コミットメッセージ.md)

| type        | 意味            | 例                           |
| ----------- | ------------- | --------------------------- |
| `feat:`     | 新機能追加         | `feat: CSVエクスポート機能を追加`      |
| `fix:`      | バグ修正          | `fix: 一覧画面の表示崩れを修正`         |
| `docs:`     | ドキュメントのみ      | `docs: READMEに起動手順を追加`      |
| `refactor:` | 動作を変えないコード整理  | `refactor: 重複処理をServiceへ移動` |
| `test:`     | テスト追加・修正      | `test: UserServiceのテストを追加`  |
| `chore:`    | 雑務・保守作業       | `chore: 不要ファイルを削除`          |
| `style:`    | コードの意味を変えない整形 | `style: インデントを修正`           |
| `perf:`     | パフォーマンス改善     | `perf: 一覧取得処理を高速化`          |
| `build:`    | ビルド関連         | `build: Angularのビルド設定を変更`   |
| `ci:`       | CI/CD関連       | `ci: GitHub Actionsを追加`     |
| `revert:`   | コミット取り消し      | `revert: "feat: CSV出力を追加"`  |

## ▼ 不具合調査

| 見たいもの         | 主な道具                         |
| ------------- | --------------------------------- |
| 画面がおかしい    | Chrome / Firefox / Safari         |
| JS例外          | DevTools Console                  |
| APIがおかしい     | DevTools Network                  |
| Stateがおかしい   | Angular DevTools / Redux DevTools |
| 描画が遅い        | Chrome Performance                |
| APIが遅い        | Network Timing                    |
| Componentの繋がり | VS Code                           |
| Angular依存関係   | VS Code + ng-maze                 |
| NgRxの繋がり      | VS Code + Redux DevTools          |
| ClearML Task     | ClearML Web                       |
| Python処理       | VS Code                           |
| コンテナ障害      | Docker/Kubernetesログ               |
| DBデータ         | DB client / Backend               |
| キャッシュ        | Redis client / Backend            |
| ブラウザ保存      | DevTools Application              |
| 認証            | Network + Application + Authコード   |

## ▼ ショートカット

### vscodeで、選択範囲を{}の基準で段階的に広げるショートカット
「Expand Selection（選択範囲の拡大）」

macOS:
Control + Shift + Command + →

Windows / Linux:
Shift + Alt + →

### ターミナル① → ② → ③ のように切り替えるショートカット

macOS:

* 次のターミナル：`Shift + Cmd + ]`
* 前のターミナル：`Shift + Cmd + [`

Windows / Linux:

* 次のターミナル：`Ctrl + PageDown`å
* 前のターミナル：`Ctrl + PageUp`

VS Code公式では `Terminal: Focus Next Terminal` / `Terminal: Focus Previous Terminal` に対応しています。([Visual Studio Code][1])

なお、「ターミナルを横にSplitしている場合」は別です。

* macOS：`Option + Cmd + ← / →`
* Windows / Linux：`Alt + ← / →`

つまり、普通に複数ターミナルを開いてタブ切り替えするなら、Macでは **`Cmd + Shift + [` / `]`** を覚えておけばOKです。

[1]: https://code.visualstudio.com/docs/terminal/basics?utm_source=chatgpt.com "Terminal Basics"

<a id="prompt-template"></a>

## ▼ プロンプト/prompt

```text
# 常用
敵対的レビューを実施し指摘は即時適用。直列で3回の敵対的レビューを実施してください

# 常用2
設計変更が発生したため、再度敵対的レビューを実施し指摘は即時適用。直列で3回の敵対的レビューを実施してください
```

- 処理の遷移を追う

```md

___from___ からの状態遷移の処理を追って遷移リストを作成してください。
到達点は「http送信」「画面への反映」「ファイル出力」などを想定しています。
欲しいのは以下のようなシンプルな追跡リストです。
例にはコンポーネントの記載がありますが、関係ない場合無理に取り込まなくて良いです。

以下は例です。特殊なリンクの記載方法は踏襲してください
````md
## nameField の 遷移
- 09. [▶️:h:D](src/app/webapp-common/shared/ui-components/inputs/inline-edit/inline-edit.component.html#34) InlineEditComponent.inlineSaved():24  
- 08. [▶️:C:D](src/app/webapp-common/shared/ui-components/inputs/inline-edit/inline-edit.component.ts#94) this.textChanged.emit(this.inlineValue()):94  
- 07. [▶️:h:D](src/app/webapp-common/experiments/dumb/experiment-info-header/experiment-info-header.component.html#14) ExperimentInfoHeaderComponent.onNameChanged():14  
- 06. [▶️:C:D](src/app/webapp-common/experiments/dumb/experiment-info-header/experiment-info-header.component.ts#121) this.experimentNameChanged.emit(name):121  
- 05. [▶️:h:D](src/app/features/experiments/containers/experiment-ouptut/experiment-output.component.html#13) ExperimentOutputComponent.updateExperimentName():13  
- 04. [▶️:D:D](src/app/webapp-common/experiments/containers/experiment-ouptut/base-experiment-output.component.ts#179) this.store.dispatch(experimentDetailsUpdated({id: this.selectedExperiment().id, changes: {name}})):179  
- 03. [▶️:E:D](src/app/webapp-common/experiments/effects/common-experiments-info.effects.ts#451) CommonExperimentsInfoEffects.updateExperimentDetails$:451  
- 02. [▶️:E:D](src/app/webapp-common/experiments/effects/common-experiments-info.effects.ts#459) ApiTasksService.tasksUpdate():459  
- 01. [▶️:S:A](src/app/business-logic/api-services/tasks.service.ts#2186) POST ${basePath}/tasks.update:2186  
````

[▶️:h:D]のような記載は

1個目（必要に応じて追加してください）

:h - html
:C - コンポーネントクラスts
:D - ディスパッチ（コンポーネントより強い）
:E - エフェクト
:S - サービス
:? - 上記以外

2個目（必要に応じて追加してください）

:D  データ受け渡し
:A  API通信
:-  何もなし（何もなしなんてことあるかな as-split とか?）

```

- もっとわかりやすく。端折るとそこでわからなくなる。子がこうで親がこうのように毎回解説

- ＊＊＊＊＊を基礎から。まずは15分コース

- レッドチームモード

これから共有する情報に関して、あなたの仕事はそれを破壊することだけ。
前提の誤り、見落とした リスク、二次的影響、失敗ポイントをすべて洗い出してください。
容赦は不要。

- 敵対的レビュー（ループエンジニアリング）
  + ___レビュー対象___に対して敵対的レビュー実施。指摘は資料化 「x-plan/A04_指摘/___資料___」
  + 「x-plan/A04_指摘/___資料___」を敵対的レビュー。細かいことではなくて、大枠でクリティカルな変なとこないか。指摘は資料に対して「外部AIによる指摘に対する指摘」という記載で追記してください
  + 「x-plan/A04_指摘/___資料___」に指摘に対する指摘を追記しました。指摘の指摘を間に受けず、再度自己精査した上で、資料全体を校正してください。
  + 再度、あなた自身で敵対的レビューを実施し指摘があれば即時修正してください。※無理に指摘を上げる必要はありません。
    * 即時修正後、再度レビューを実施するというループエンジニアリングを全３回まで実施してください。
  + OKです、では指摘資料に則って、___レビュー対象___を修正更新してください。

- UI/UX レビューの提案

  一般的なUI,UXの考え方に則りフロントエンドの実装・挙動に関して
  ・UX体験が向上する
  ・LM体験が向上する
  ・上記二分以外の観点
  を網羅的に徹底的に提案する
  
  アプリとしての成熟度を上げるため
  UXの基本・応用・ユースケースの情報を収集した上で
  改善案、修正案を提示してください。
  
  提案内容は、内容ごとに{___ファイルパス___}にファイル化する。

- 最速で関数の役割を理解する

以下の手段で私にイメージさせてください
「自分ならこの処理に何というメソッド名を付けるか」で考えて仮命名すると理解が早く認識の裏付けが取れる

| 正式API            | 頭の中の仮メソッド名                     |
| ---------------- | ------------------------------ |
| `toSignal()`     | `observableToSignal()`         |
| `resource()`     | `asyncLogicToResource()`       |
| `rxResource()`   | `observableLogicToResource()`  |
| `httpResource()` | `httpLogicToResource()`        |
| `computed()`     | `signalsToDerivedValue()`      |
| `linkedSignal()` | `sourceLinkedWritableSignal()` |
| `effect()`       | `signalChangeToSideEffect()`   |


## ▼ windows11

- 便利熟語
  -「簡易答」

- キャッシュ
C:\USES\\{USER_NAME}\AppData\Local\Temp

## ▼ 正規表現

- and検索
  (?=.*abc)(?=.*DEF)

- not検索
  (?!abc)

- 改行削除
  (\r\n|\r|\n)

- 

## ▼ plantUMLやmadgeで図を書く場合
```bash
sudo apt install graphviz
```

## ▼ Angular関係

### madge

````markdown
基本の使い方
この構成では --extensions ts と --ts-config がほぼ必須です。付けないと .ts ファイルが対象にならず、@common/... のような path alias も解決されません。

# 循環依存の検出
madge --circular --extensions ts --ts-config tsconfig.app.json src/app

# 指定ファイルをimportしているファイル一覧（パスは src/app からの相対）
madge --depends webapp-common/shared/utils/tableParamEncode.ts --extensions ts --ts-config tsconfig.app.json src/app

# 誰からもimportされていないファイル
madge --orphans --extensions ts --ts-config tsconfig.app.json src/app

# 何もimportしていない末端のファイル
madge --leaves ...

# ファイルごとの依存数
madge --summary ...

# 特定ファイルを起点にした依存図
madge --image deps.svg --extensions ts --ts-config tsconfig.app.json src/app/features/experiments/xxx.component.ts

# 結果をJSONで出力（他のツールに渡す場合）
madge --json ...

---

# madge が自分でファイルを書き出すのは --image だけ（Graphviz が必要）
madge --image deps.svg ...

# それ以外はリダイレクトで保存
madge --circular ... > circular.txt
madge --json ...     > deps.json
madge --dot ...      > deps.dot   # Graphviz 用の DOT 形式
````

## ▼ Linux/Ubuntu

- 現在階層は以下を検索  
```bash
grep -RIl "探したい文字列" .
```

- ファイルの中身ではなく「ファイル名に特定文字を含むもの」を探したい場合  
```bash
find . -type f -iname '*experiment*'
```

## ▼ デバッグ

- chrome起動
  open -na "Google Chrome" --args --remote-debugging-port=9222 --user-data-dir=/tmp/angular22-chrome-debug  
