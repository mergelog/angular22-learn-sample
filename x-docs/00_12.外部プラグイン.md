# 00_12. 外部プラグイン（関数レベル一覧）

## この資料の前提

- 対象コードベース: `/Users/yasu/work/mergelog/learn-ClearML-pro`（`src/` 配下）
- **実際に import されている関数・クラスのみ** を掲載（ライブラリが提供する全 API ではない）
- 対象外: `@angular/*` / `@ngrx/*` / `rxjs` / `primeng` / `chart.js` など UI・状態管理の基盤ライブラリ
- 各表の行は **使用箇所の多い順**。上にあるものから覚えると費用対効果が高い
- 元調査: [04_lodash以外の外部汎用モジュール調査](../x-outside/04_lodash以外の外部汎用モジュール調査)

---

## 1. lodash-es（汎用ユーティリティ / 34 関数）

ESM 版 lodash。型は `@types/lodash-es`。ClearML では
**「値の比較」「安全な取り出し」「深いコピー」** が主用途で、
Angular の変更検知と NgRx のイミュータブル更新を支える方向に偏っている。

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| lodash-es | `isEqual` | 比較 | 2 値を再帰的に深く比較。変更検知・`distinctUntilChanged` の判定に多用 | [公式](https://lodash.com/docs/4.17.15#isEqual) |
| lodash-es | `get` | オブジェクト | `a.b[0].c` のようなパスを安全に取得。存在しなければ既定値 | [公式](https://lodash.com/docs/4.17.15#get) |
| lodash-es | `cloneDeep` | オブジェクト | 深いコピーを作る。NgRx の state を壊さず加工するため | [公式](https://lodash.com/docs/4.17.15#cloneDeep) |
| lodash-es | `merge` | オブジェクト | ネストしたオブジェクトを再帰マージ（破壊的） | [公式](https://lodash.com/docs/4.17.15#merge) |
| lodash-es | `last` | 配列 | 配列の最後の要素を取得 | [公式](https://lodash.com/docs/4.17.15#last) |
| lodash-es | `pick` | オブジェクト | 指定キーだけを抜き出した新オブジェクトを作る | [公式](https://lodash.com/docs/4.17.15#pick) |
| lodash-es | `uniqBy` | 配列 | 判定関数の結果が同じ要素を除いて一意化（id 単位の重複排除） | [公式](https://lodash.com/docs/4.17.15#uniqBy) |
| lodash-es | `uniq` | 配列 | 値の重複を除いて一意化 | [公式](https://lodash.com/docs/4.17.15#uniq) |
| lodash-es | `isUndefined` | 型判定 | `undefined` か判定（`null` は false） | [公式](https://lodash.com/docs/4.17.15#isUndefined) |
| lodash-es | `flatten` | 配列 | 1 段だけ平坦化 | [公式](https://lodash.com/docs/4.17.15#flatten) |
| lodash-es | `camelCase` | 文字列 | `foo_bar` → `fooBar`。API の snake_case を JS 側へ変換 | [公式](https://lodash.com/docs/4.17.15#camelCase) |
| lodash-es | `isEmpty` | 型判定 | 空判定。`{}` `[]` `''` `null` をまとめて見る | [公式](https://lodash.com/docs/4.17.15#isEmpty) |
| lodash-es | `isNil` | 型判定 | `null` または `undefined` か判定 | [公式](https://lodash.com/docs/4.17.15#isNil) |
| lodash-es | `escapeRegExp` | 文字列 | 正規表現の特殊文字をエスケープ。検索語を安全に RegExp 化 | [公式](https://lodash.com/docs/4.17.15#escapeRegExp) |
| lodash-es | `sortBy` | コレクション | キー抽出関数で安定ソート（常に昇順） | [公式](https://lodash.com/docs/4.17.15#sortBy) |
| lodash-es | `capitalize` | 文字列 | 先頭だけ大文字、残りは小文字。ラベル表示用 | [公式](https://lodash.com/docs/4.17.15#capitalize) |
| lodash-es | `unionBy` | 配列 | 複数配列を判定関数基準で重複なく結合 | [公式](https://lodash.com/docs/4.17.15#unionBy) |
| lodash-es | `escape` | 文字列 | `&<>"'` を HTML エンティティへ変換（XSS 対策の一次防御） | [公式](https://lodash.com/docs/4.17.15#escape) |
| lodash-es | `min` | 数値 | 配列の最小値 | [公式](https://lodash.com/docs/4.17.15#min) |
| lodash-es | `set` | オブジェクト | パス指定で値を書き込む（途中の階層は自動生成・破壊的） | [公式](https://lodash.com/docs/4.17.15#set) |
| lodash-es | `has` | オブジェクト | パスがキーとして存在するか判定 | [公式](https://lodash.com/docs/4.17.15#has) |
| lodash-es | `castArray` | 配列 | 単値でも配列でも必ず配列にそろえる | [公式](https://lodash.com/docs/4.17.15#castArray) |
| lodash-es | `mergeWith` | オブジェクト | マージ規則を自前関数で指定できる `merge`（配列の扱いを変えたい時） | [公式](https://lodash.com/docs/4.17.15#mergeWith) |
| lodash-es | `isArray` | 型判定 | 配列か判定（`Array.isArray` 相当） | [公式](https://lodash.com/docs/4.17.15#isArray) |
| lodash-es | `parseInt` | 数値 | 基数を厳密に扱う `parseInt`。環境差を吸収 | [公式](https://lodash.com/docs/4.17.15#parseInt) |
| lodash-es | `template` | 関数 | 文字列テンプレートをコンパイルして関数化 | [公式](https://lodash.com/docs/4.17.15#template) |
| lodash-es | `omit` | オブジェクト | 指定キーを除いた新オブジェクトを作る（`pick` の逆） | [公式](https://lodash.com/docs/4.17.15#omit) |
| lodash-es | `findLastIndex` | 配列 | 条件に合う最後の要素の index を返す | [公式](https://lodash.com/docs/4.17.15#findLastIndex) |
| lodash-es | `max` | 数値 | 配列の最大値 | [公式](https://lodash.com/docs/4.17.15#max) |
| lodash-es | `zip` | 配列 | 複数配列を index ごとに組にまとめる | [公式](https://lodash.com/docs/4.17.15#zip) |
| lodash-es | `chunk` | 配列 | 指定サイズごとに分割（バッチリクエスト用） | [公式](https://lodash.com/docs/4.17.15#chunk) |
| lodash-es | `slice` | 配列 | 範囲切り出し。疎配列も正しく扱う | [公式](https://lodash.com/docs/4.17.15#slice) |
| lodash-es | `snakeCase` | 文字列 | `fooBar` → `foo_bar`。API へ送る時の変換 | [公式](https://lodash.com/docs/4.17.15#snakeCase) |
| lodash-es | `orderBy` | コレクション | 複数キー＋昇降順を指定してソート（`sortBy` の上位版） | [公式](https://lodash.com/docs/4.17.15#orderBy) |

---

## 2. ngxtension（Angular Signal 用ユーティリティ）

lodash が「JS の汎用」なら、こちらは「Angular Signal の汎用」。
**サブパス単位で import** するのが作法（`ngxtension/xxx`）。

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| ngxtension | `computedPrevious` | Signal | Signal の「前回値」を保持した Signal を作る。差分検知やアニメーション起点に | [公式](https://ngxtension.dev/utilities/signals/computed-previous/) |
| ngxtension | `injectResize` | DOM 購読 | 要素サイズ変更を `Observable` で購読（ResizeObserver のラッパー） | [公式](https://ngxtension.dev/utilities/directives/resize/) |
| ngxtension | `explicitEffect` | Signal | 依存 Signal を明示指定する `effect`。暗黙依存による意図しない再実行を防ぐ | [公式](https://ngxtension.dev/utilities/signals/explicit-effect/) |
| ngxtension | `injectQueryParams` | ルーティング | URL のクエリパラメータを Signal 化。`?x=1` の変化に追従 | [公式](https://ngxtension.dev/utilities/injectors/inject-query-params/) |
| ngxtension | `injectParams` | ルーティング | ルートパラメータ（`:id` など）を Signal 化 | [公式](https://ngxtension.dev/utilities/injectors/inject-params/) |
| ngxtension | `NgxResize` / `ResizeResult` | DOM 購読 | ディレクティブ版のリサイズ購読と、その結果型 | [公式](https://ngxtension.dev/utilities/directives/resize/) |
| ngxtension | `injectRouteData` | ルーティング | ルートの `data` を Signal 化 | [公式](https://ngxtension.dev/utilities/injectors/inject-route-data/) |
| ngxtension | `mapArray` | RxJS | 配列ストリームの各要素に `map` を適用する演算子（`map(arr => arr.map(f))` の短縮） | [公式](https://ngxtension.dev/utilities/operators/map-array/) |

---

## 3. 準汎用（領域ユーティリティ）層

### 3-1. date-fns（日付）

関数単位 import の日付ライブラリ。immutable で、moment と違い元の `Date` を壊さない。

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| date-fns | `format` | 整形 | `Date` を指定パターンの文字列へ（`yyyy-MM-dd HH:mm` など） | [公式](https://date-fns.org/docs/format) |
| date-fns | `parseISO` | 解析 | ISO 8601 文字列を `Date` に変換。API のタイムスタンプ受け取り | [公式](https://date-fns.org/docs/parseISO) |
| date-fns | `addDays` | 加算 | 日数を加算した新しい `Date` を返す | [公式](https://date-fns.org/docs/addDays) |
| date-fns | `startOfDay` | 丸め | その日の 00:00:00 に丸める。期間フィルタの下限に使う | [公式](https://date-fns.org/docs/startOfDay) |
| date-fns | `subSeconds` | 減算 | 秒を引く。「直近 N 秒」の範囲計算 | [公式](https://date-fns.org/docs/subSeconds) |
| date-fns | `isValid` | 判定 | 有効な日付か判定（`Invalid Date` 検出） | [公式](https://date-fns.org/docs/isValid) |
| date-fns | `formatDistance` | 整形 | 「3 分前」のような相対表現を生成 | [公式](https://date-fns.org/docs/formatDistance) |
| date-fns | `enGB`（locale） | ロケール | 英国式ロケール。日付の曜日・順序の基準 | [公式](https://date-fns.org/docs/I18n) |

### 3-2. uuid（ID 生成）

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| uuid | `v4` | ランダム | 乱数ベースの UUID。一時 ID・DOM の一意キー生成 | [公式](https://github.com/uuidjs/uuid#uuidv4options-buffer-offset) |
| uuid | `v5` | ハッシュ | 名前空間＋名前から **決定的に** 同じ UUID を作る。同入力→同 ID が必要な場面 | [公式](https://github.com/uuidjs/uuid#uuidv5name-namespace-buffer-offset) |
| uuid | `v1` | 時刻 | タイムスタンプベース。生成順が UUID の並びに反映される | [公式](https://github.com/uuidjs/uuid#uuidv1options-buffer-offset) |

### 3-3. @ctrl/tinycolor（色）

`tinycolor2` の TypeScript 移植。`new TinyColor(色)` を起点にメソッドチェーンする。

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| @ctrl/tinycolor | `new TinyColor()` | 生成 | hex / rgb オブジェクト / hsl 文字列など雑多な入力を 1 つの色オブジェクトへ正規化 | [公式](https://tinycolor.vercel.app/) |
| @ctrl/tinycolor | `.toRgb()` | 変換 | `{r,g,b,a}` オブジェクトで取り出す | [公式](https://tinycolor.vercel.app/#torgb) |
| @ctrl/tinycolor | `.toHexString()` | 変換 | `#rrggbb` 文字列へ。設定保存・CSS 出力用 | [公式](https://tinycolor.vercel.app/#tohexstring) |
| @ctrl/tinycolor | `.toRgbString()` | 変換 | `rgb(r, g, b)` 文字列へ | [公式](https://tinycolor.vercel.app/#torgbstring) |
| @ctrl/tinycolor | `.mix()` | 加工 | 2 色を指定比率で混ぜる。系列色のグラデーション生成 | [公式](https://tinycolor.vercel.app/#mix) |
| @ctrl/tinycolor | `.lighten()` | 加工 | 明度を上げる（グラフの枠線色など） | [公式](https://tinycolor.vercel.app/#lighten) |
| @ctrl/tinycolor | `.shade()` | 加工 | 黒を混ぜて暗くする | [公式](https://tinycolor.vercel.app/#shade) |
| @ctrl/tinycolor | `.setAlpha()` | 加工 | 透明度を設定 | [公式](https://tinycolor.vercel.app/#setalpha) |
| @ctrl/tinycolor | `.toHsl()` | 変換 | `{h,s,l}` で取り出す。色相だけずらす処理に使う | [公式](https://tinycolor.vercel.app/#tohsl) |
| @ctrl/tinycolor | `.isDark()` | 判定 | 暗い色か判定。文字色を白/黒どちらにするかの分岐 | [公式](https://tinycolor.vercel.app/#isdark) |
| @ctrl/tinycolor | `.getLuminance()` | 計測 | WCAG 相対輝度を返す。コントラスト計算の土台 | [公式](https://tinycolor.vercel.app/#getluminance) |
| @ctrl/tinycolor | `.getBrightness()` | 計測 | 0〜255 の知覚的明るさ | [公式](https://tinycolor.vercel.app/#getbrightness) |
| @ctrl/tinycolor | `mostReadable` | 判定 | 背景色に対し候補色の中から最も読みやすい色を選ぶ。タグ(chip)の文字色決定 | [公式](https://tinycolor.vercel.app/#mostreadable) |

### 3-4. dompurify（HTML サニタイズ）

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| dompurify | `DOMPurify.sanitize` | セキュリティ | HTML から script などの危険要素を除去。`ADD_TAGS`/`FORBID_ATTR` で許可範囲を制御 | [公式](https://github.com/cure53/DOMPurify#can-i-configure-dompurify) |
| dompurify | `DOMPurify.addHook` | セキュリティ | サニタイズ処理に割り込む。ClearML は `iframe` の `src` を自ドメイン以外なら空にしている | [公式](https://github.com/cure53/DOMPurify#hooks) |

### 3-5. semver（バージョン比較）

`semver/functions/xxx` のサブパス import で、パッケージ全体は取り込まない。

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| semver | `valid` | 検証 | セマンティックバージョンとして妥当か（不正なら `null`）。フォーム検証に使用 | [公式](https://github.com/npm/node-semver#functions) |
| semver | `gt` | 比較 | `a > b` 判定。サーバ版数が要求より新しいかの判定 | [公式](https://github.com/npm/node-semver#functions) |
| semver | `gte` | 比較 | `a >= b` 判定 | [公式](https://github.com/npm/node-semver#functions) |

### 3-6. filesize（バイト数整形）

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| filesize | `filesize` | 整形 | `1536` → `1.5 KB` のように人間可読化。pipe とグラフ軸ラベルで使用 | [公式](https://filesizejs.com) |
| filesize | `FilesizeOptions` | 型 | 単位系・小数桁などのオプション型 | [公式](https://filesizejs.com) |

### 3-7. @ngneat/dag（DAG レイアウト計算）

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| @ngneat/dag | `DagManagerService` | グラフ | ノード配列から DAG の段組み（何段目の何番目か）を計算するサービス。パイプライン図の土台 | [公式](https://www.npmjs.com/package/@ngneat/dag) |
| @ngneat/dag | `DagModelItem` | 型 | DAG ノードが満たすべき最小インターフェース（`stepId` / `parentIds`）。自前の型が extends する | [公式](https://www.npmjs.com/package/@ngneat/dag) |

---

## 4. 単機能層（1〜2 ファイル専用）

| プラグイン名 | 関数名 | カテゴリ | 処理目的 | 公式リンク |
| --- | --- | --- | --- | --- |
| marked | `marked` | Markdown | Markdown → HTML 変換器。ClearML は `window['marked']` に載せて `ngx-markdown-editor` に渡す（直接呼ばない） | [公式](https://marked.js.org/) |
| diff | `Diff.diffArrays` | 差分 | 2 配列を厳密等価で比較し、追加/削除/共通のブロック列を返す。実験比較の差分ハイライト | [公式](https://github.com/kpdecker/jsdiff#api) |
| lucene | `lucene.parse` | 構文解析 | Lucene クエリ文字列を AST に変換。検索条件の解析 | [公式](https://github.com/bripkens/lucene#usage) |
| lucene | `lucene.isValid` | 構文解析 | Lucene クエリとして妥当か判定（入力欄のバリデーション） | [公式](https://github.com/bripkens/lucene#usage) |
| hocon-parser | `parseHocon` | 構文解析 | HOCON（Typesafe Config 形式）文字列を JS オブジェクトへ。モデル設定の読み取り | [公式](https://github.com/yellowblood/hocon-js) |
| export-to-csv | `mkConfig` | CSV | ファイル名・ヘッダ有無などの CSV 設定オブジェクトを作る | [公式](https://github.com/alexcaza/export-to-csv#api) |
| export-to-csv | `generateCsv` | CSV | 設定を部分適用した関数に行データを渡して CSV を生成（カリー化 API） | [公式](https://github.com/alexcaza/export-to-csv#api) |
| export-to-csv | `asString` | CSV | 生成結果を文字列として取り出す（クリップボード用） | [公式](https://github.com/alexcaza/export-to-csv#api) |
| export-to-csv | `download` | CSV | 生成結果をブラウザのダウンロードとして保存させる | [公式](https://github.com/alexcaza/export-to-csv#api) |
| dom-to-image | `domtoimage.toBlob` | 画像化 | DOM 要素を SVG 経由でラスタ画像の `Blob` に変換。グラフのダウンロード | [公式](https://github.com/tsayen/dom-to-image) |
| ansi-to-html | `new Convert()` | ログ整形 | ANSI エスケープ変換器のインスタンス生成（色設定を保持） | [公式](https://github.com/rburns/ansi-to-html) |
| ansi-to-html | `convert.toHtml` | ログ整形 | ANSI カラーコードを `<span style>` 付き HTML へ。コンソールログ表示 | [公式](https://github.com/rburns/ansi-to-html) |
| has-ansi | `hasAnsi` | ログ整形 | 文字列に ANSI エスケープが含まれるか判定。変換が要るかの事前チェック | [公式](https://github.com/chalk/has-ansi) |
| string-to-color | `stc` | 色 | 任意の文字列から決定的に色を決める。ラベル名→色の自動割り当て | [公式](https://github.com/Gustu/string-to-color) |
| d3-selection | `select` | DOM | セレクタ/要素から d3 セレクションを作る。`.on('click', ...)` でイベント購読（Plotly のツールバーボタンに後付け） | [公式](https://d3js.org/d3-selection/selecting) |
| d3-interpolate | `interpolateBasis` | 補間 | 値の配列を通る B スプライン補間関数を作る。グラフの平滑化 | [公式](https://d3js.org/d3-interpolate/value) |
| curved-arrows | `getBoxToBoxArrow` | 座標計算 | 2 つの矩形の間を結ぶ曲線矢印の制御点を算出。パイプライン図のノード間矢印 | [公式](https://github.com/dragonman225/curved-arrows) |
| url | `parse` | polyfill | Node の `url.parse` をブラウザで使う。`s3://` スキームの解析（`URL` では扱いにくい） | [公式](https://nodejs.org/api/url.html#urlparseurlstring-parsequerystring-slashesdenotehost) |
| url | `UrlWithStringQuery` | 型 | `parse` の戻り値型 | [公式](https://nodejs.org/api/url.html#legacy-urlobject) |

---

## 5. 表に載らないもの（注意点）

| プラグイン名 | 状況 | 公式リンク |
| --- | --- | --- |
| object-hash | `package.json` にあるが `src/` からの参照が 0 の完全未使用。公式フォーク時の残骸 | [公式](https://github.com/puleos/object-hash) |
| taira | 同じく未使用。learn-ClearML-pro 側の `angular.json` の `allowedCommonJsDependencies` に名前だけ残る。オブジェクトのフラット化用で lodash と役割が被った | [公式](https://github.com/petoem/taira) |
| plotly.js | `import` は 9 ファイルあるが **型定義専用**。実行時は `<script>` 動的挿入＋`declare let Plotly` のグローバル参照でバンドルに含まれない | [公式](https://plotly.com/javascript/) |

補足: `@ngneat/dag` の GitHub リポジトリ（`github.com/ngneat/dag`）は現在 404。
API を確認したい場合は npm ページか `node_modules/@ngneat/dag/README.md` を見るのが確実。

---

## 6. 学習の優先順位（おすすめ）

1. **lodash-es の上位 6 つ** — `isEqual` `get` `cloneDeep` `merge` `pick` `uniq/uniqBy`。
   これだけで lodash 使用箇所の 8 割近くを読める
2. **ngxtension の `computedPrevious` / `explicitEffect` / `injectResize`** — Angular Signal 時代の定番。自分のコードでも使える
3. **date-fns** — 関数単位 import という作法ごと覚える価値がある
4. 残りは「読むとき引く」で十分。単機能層は用途が名前どおりなので暗記不要
