# 購読終了時に後片付けを実行

## 関数詳細

`finalize(() => ...)` は Observable の完了・エラー・購読解除時に処理を1回実行する。

## どういうケースで使用するか

通信終了時に、成功・失敗・キャンセルを問わずローディング表示を消したい場合。

## 注意点

値が流れるたびには実行されない。購読が続く間は実行されないため、終わらない Observable では購読解除も必要。
