# 連続する同じキーの値を除く

## 関数詳細

`distinctUntilKeyChanged('category')` は最初の値を流し、以降は直前に流した値と `category` が異なる場合だけ流す。例の出力は `A(1) → B(3) → A(4)`。

## どういうケースで使用するか

オブジェクトの特定の項目が変わったときだけ通知したい場合。

## 注意点

`id` が変わっても `category` が同じなら流れない。離れて現れた同じ `category` は再び流れる。
